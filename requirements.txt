# Runtime dependencies for the Huawei Cup skill.
# Keep this machine-readable file ASCII-only for Windows tooling that uses the
# active ANSI code page instead of UTF-8. Human-facing notes live in the docs.

# Required: PDF and document parsing
pymupdf>=1.28.2          # audit_paper.py, submission_audit.py, corpus_build.py
pypdf>=6.19.0            # corpus_build.py, submission_audit.py

# Required: DOCX generation and package inspection
python-docx>=1.1.2        # build_docx.py
lxml>=6.1.0               # build_docx.py, audit_docx.py

# Required: tests, YAML, validation, and figures
pytest>=8.0               # tests/
pyyaml>=6.0.3             # render_roadmap.py, audit_roadmap.py
matplotlib>=3.8           # render_roadmap.py, doctor.py
jsonschema>=4.20          # audit_roadmap.py

# Figure assets and MATLAB .mat interoperability
numpy>=2.0
pandas>=2.0
scipy>=1.10
