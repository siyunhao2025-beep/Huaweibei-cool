# 华为杯研赛 Skill 交付说明（v0.4.1）

本仓库是一个面向**中国研究生数学建模竞赛（华为杯）**的辅助 Skill：把 729 篇历年获奖论文蒸馏成
可检索的知识资产，并提供一套比赛日工作流脚本、论文章节模板与质量门禁脚本。
它**不替你解题、不承诺获奖、不代提交**；所有 AI 产出须你理解后用，并按当届官方规定披露。

---

## 一、语料覆盖率

| 指标 | 数值 |
|---|---|
| 论文索引 | 729 条（`corpus/papers_index.json`，字段齐全、paper_id 唯一） |
| 正文抽文 | 729 / 729（`extract_status=ok`，抽文文件全部落盘） |
| 简卡 brief | 729 张（`corpus/cards/brief/<年>.json`，覆盖 2004–2024 共 21 年） |
| 深卡 deep | 80 张（`corpus/cards/deep/`） |
| 聚合统计 | 67 类方法频次（`method_frequency.json` / `track_archetype_matrix.json`） |

**深卡按年分布**：2024 年 24 张、2021 年 18 张、2014/2022 各 5 张、2017/2018 各 3 张，
其余年份各 1–2 张——深卡向近年与重点年份倾斜，符合“近年方法更可迁移”的取舍。

---

## 二、知识产物清单

- **8 大题型原型 playbooks**：optimization / evaluation / prediction / classification-cv /
  mechanism / signal / spatial-graph / simulation；每原型下有若干方法卡（`playbooks/<原型>/*.md`），
  匹配规则见 `playbooks/match_rules.json`。
- **24 题赛道档案**：`tracks/<2022-2025>/`，每年 A–F 共 6 题，含题面拆解、数据模态、
  官方硬约束、原型映射与方法谱系。
- **20 个工作流模块**（`modules/`）：选题、读题审计、问题分型、求解、验证、证据台账、
  创新、摘要、写作、降 AI、评审、投稿等。
- **13 份文档**（`docs/`）：架构、阶段门禁、评分 rubric、赛道档案说明、上游来源、
  技术路线图烹饪手册、排障手册、v0.3 对抗评审记录等。
- **论文模板**（`assets/paper-template/`）：`gmcmthesis.cls` + `example.tex` + `reference.bib`
  + 章节模板 + Word 模板；**比赛日骨架**（`assets/scaffold/`）：题目/数据/求解/论文/提交附件五区。
- **学术配图 Skill**（`skills/academic-figure/`）：SKILL.md + scripts + 19 张参考 PNG（13MB）。
- **优秀论文自检表**（`assets/checklists/`）：`paper_checklist.json`（138 条结构化机检条目）+ `优秀论文自检表.md`（人读版），
  由第三方优秀论文自检表图片转录固化；机检脚本 `scripts/paper_checklist.py` 在 P4/P6 门禁中闭环。

### 反 AI 读题审计机制（核心纪律）

- 读题**禁止只读摘要就开干**：按原文顺序逐段逐句产出**四元组**（原文摘录 → 自己复述 → 约束 Cx → 疑问 Qx），
  并形成 7 节读题审计报告（逐段拆解表 / 任务大纲 / 约束与假设 / 陷阱与歧义 / 建模候选 / 图表计划 / 开放项）。
- **人工确认硬门禁（P1 退出条件）**：读题审计报告须先呈现给用户、**等用户明确确认后才允许进入求解**；
  用户裁决与确认写入 `evidence-ledger`。`scripts/progress.py --gate P1` 会检查工作目录中是否存在
  `evidence-ledger.json`（`user_confirmation=true`）或含确认标记的 markdown 台账；
  **无确认记录 → P1 FAIL，禁止进入 P2 求解**。判型器只出候选，未经质证+确认不采信。

---

## 三、脚本与工程验证结果

`scripts/` 下 22 个 `.py` 脚本，`--help` **全部退出码 0**：
audit_docx / audit_paper / audit_roadmap / audit_tex / blind_check / build_docx / build_latex /
calibrate_page_targets / contest_init / corpus_build / corpus_cards / doctor / evidence /
migrate_markdown_to_tex / paper_checklist / playbook_match / progress / render_roadmap / review /
submission_audit / visual_plan_audit / visual_plan_init。

关键功能实测：

| 验证项 | 结果 |
|---|---|
| `corpus_cards.py --stats` | 可复现：briefs=729 deeps=80 methods=67 |
| `playbook_match.py`（2024 A 题面） | 主原型 optimization=0.99，辅 prediction，与赛道档案一致 |
| `progress.py`（空目录） | P0 全缺 → FAIL（正确） |
| `progress.py`（scaffold / dry-run） | 放 contest.json 后 P0 PASS；≥3 条假设后 P2 PASS |
| `progress.py --gate P1` | 无确认记录 FAIL、有 evidence-ledger 确认记录 PASS（正反例均验证） |
| `submission_audit.py`（example.tex/PDF） | 正常运行，检出身份标志/缺 AI 披露（预期） |
| `audit_paper.py`（编译后 PDF） | 12 页，检出身份文本/无标题清单（预期 FAIL，脚本本身不崩） |
| **xelatex 编译 example.tex** | **成功，输出 12 页 PDF** |

测试：`python -m pytest tests/ -v` → **87 passed, 2 skipped, 1 xfailed**（全绿，无 failure）。
2 skipped 为 MATLAB 真机测试（未设 `HUAWEI_RUN_MATLAB=1` 时自动跳过，不计失败）；1 xfailed 为已知薄弱题 2017_B（诚实标注的预期失败）。
覆盖：索引完整性、卡片 schema、P0–P2 门禁正反例、P1 确认门禁正反例、playbook 匹配、submission_audit、audit_tex、figure skill 与中文字体、
MATLAB smoke（skip）、**30 题盲检 golden set**（含 8 原型全覆盖 / 命中率阈值 / 3 已知陷阱全对）、
**技术路线图 12 项**（schema 正反例 / 8 模板 / 3 demo 渲染非空 / 无 CJK 字体英文回退 / --help）、
**doctor 自检 4 项**（脚本存在 / --help 退出 0 / JSON shape / 核心检查不 FAIL）、
**优秀论文自检表机检 7 项**（JSON 138 条 / 人读版 / --help / pass·fail fixture / --mark+strict / contest_init 复制）、
**P4/P6 门禁 6 项正反例**（自检表存在性 / 已勾选表 / sidecar 未裁决计数）。

> 中文字体：现场检出系统可用 CJK 字体（Microsoft YaHei 等），xelatex 中文渲染链路通过。

### Wave4 真机与盲检新增实证（v0.2.0）

| 验证项 | 结果 |
|---|---|
| **MATLAB R2024b 真机** | 113 工具箱检出；Optimization / Global Opt / Statistics / Signal / Image 均 available；`linprog`/`intlinprog`/`fmincon`/`ga`/`ode45` 结果符合预期；`.mat`/`.csv` 与 Python 回读一致；中文出图无方块 |
| MATLAB smoke 脚本 | `scripts/matlab_smoke.m` + `tests/test_matlab_smoke.py`（默认 SKIP）+ `tests/fixtures/matlab_smoke_R2024b.log` |
| 模板 loglo.png 缺陷 | 已删除装饰图块，干净目录 xelatex 双遍编译 12 页 PDF 通过 |
| **Word 链路真机烟雾** | python-docx + audit_docx + `render_word.vbs` 真机导出 8 页 PDF（本机 Word 16.0） |
| 盲检 golden set | 30 题（8 原型全覆盖 + 3 已知陷阱），`match_rules.json` 调优后命中率 **97%（29/30）**，陷阱 3/3 |
| 奖级口径对账 | 615 vs 722 根因查清（两奖级存储分叉）；729 篇自述式奖级扫描（1 一等奖 + 12 数模之星提名） |
| 禁忌搜索标签 | 泛化标签清洗 61 → 4（57 篇改标"启发式优化"） |
| 隐私/品牌 grep | 使用者真实姓名/拼音/导师/单位等身份词、旧内部品牌代号、真实广告联系方式（微信号/QQ/广告 ID）面向用户文件**零命中** |

### Wave5 集成新增实证（v0.3.0）

| 验证项 | 结果 |
|---|---|
| 技术路线图渲染 | 3 个 demo YAML 重跑 `render_roadmap.py`，PNG 100–170 KB / PDF 50–75 KB 非空白；8 原型模板 `audit_roadmap.py` 全 PASS |
| doctor.py 本机自检 | 8 PASS / 1 WARN（graphviz dot 可选未装）/ 0 FAIL / 0 SKIP |
| quickstart demo 全链路 | `quickstart_demo.ps1` 跑通：doctor → contest_init → playbook_match（optimization 0.9）→ render_roadmap → xelatex 43.7 KB PDF |
| 干净 xelatex 编译 | 临时目录复制 paper-template，xelatex×3 + bibtex 出 **12 页 PDF（602 KB）** |
| 隐私/品牌 grep（含 untracked） | 身份词/旧品牌词/广告 ID 在工作区文本文件**零命中**（含本次新增的 doctor.py / ci.yml / REVIEW_v0.3.md / contest.schema.json） |
| CI workflow | `.github/workflows/ci.yml`：Ubuntu + Python 3.12，doctor 步骤 continue-on-error（runner 无 xelatex/MATLAB），pytest 主步骤 |
| MATLAB Deep Learning license | `matlab_smoke.m` 加 license 探测块；本机 R2024b 实测 DL license **unavailable**（如实记录，不假装可用） |

### Wave6 集成新增实证（v0.4.0）

| 验证项 | 结果 |
|---|---|
| 全量 pytest | **87 passed / 2 skipped / 1 xfailed**（MATLAB skip、2017_B xfail） |
| doctor.py 本机自检 | 8 PASS / 1 WARN（graphviz dot 可选未装）/ 0 FAIL / 0 SKIP |
| paper_checklist 正例 fixture | `checklist_pass.tex` → 统计 ✅17 ❌0 ☐118 ➖3，**退出码 0**（无机检 ❌） |
| paper_checklist 反例 fixture | `checklist_fail.tex` → 检出 **8 个 ❌**：H05（假设 3≠问题数 2）、Q02（图宽 0.99>0.95）、Q03（1 个题注纯 ASCII）、Q04（2 个 label 未引用）、Q09（求解章含 \textbf）、V04（优化章缺"灵敏度"）、E03（优点 1 不大于缺点 2）、R02（参考文献含 DOI），**退出码 1** |
| quickstart demo 全链路 | `quickstart_demo.ps1` 跑通：doctor → contest_init（已自动复制自检表）→ playbook_match（optimization 0.9）→ render_roadmap → xelatex 43.8 KB PDF |
| demo 论文跑 paper_checklist | 如实检出 **1 ❌**（V04 缺灵敏度；玩具稿非 100% 合规，按约定不修 demo），其余机检项 ✅/待人工 |
| 3 张路线图重渲染 | classification / mechanism / optimization 三 demo 重跑 `render_roadmap.py`，PNG+PDF 非空 |
| 新脚本 --help / 文档命令抽查 | `paper_checklist.py --help` 退出 0；render_roadmap / progress / contest_init / playbook_match / audit_tex / submission_audit / corpus_build 共 7 条 --help 全退出 0 |
| 隐私/品牌 grep（含 untracked） | 身份词（姓名/拼音/导师/单位/专业/GitHub 拼音）、旧品牌词、广告 ID（85309976/kuge225/wxid_）**零命中**；华为杯面向用户的 `assets/checklists/` 与 `modules/` 无商业 AI 产品名残留（单测 test_paper_checklist.py 强制） |

> ✅ **F03 字体已按官方宋体定稿（2026-09-21 用户裁决）**：第三方自检表原要求二三级标题"小四黑体"，经裁决**不采纳**；以华为杯官方格式规范为准（小四宋体），与 `gmcmthesis.cls`/Word 模板现状一致，**不改模板**。裁决依据与官方出处见 `modules/paper-writing.md` §6.1 与 `docs/CHANGELOG.md` v0.4.1。

#### 隐私与品牌 grep 复现命令（v0.2.0 验证记录）

对全部跟踪文本文件（`.md/.py/.json/.tex/.cls/.bst/.sty/.vbs/.bat/.txt/.jsonl/.csv`，`corpus/text/` 与 `_work/` 本就 gitignore 不入库）执行：

```powershell
# 身份词：使用者真实中文姓名、英文/拼音名、导师姓名、所在单位/实验室/专业、
#         GitHub 账号名拼音、旧内部品牌代号（具体词表不在公开文档中列出）
Select-String -Path <跟踪文本文件> -Pattern '<身份词清单>' -SimpleMatch
# 真实联系方式（广告 ID 已清洗后复查）
Select-String -Path <跟踪文本文件> -Pattern '微信[:：]\s*[0-9a-zA-Z]{5,}',
  '加微\s*[0-9a-zA-Z]{5,}','QQ[:：]\s*[0-9]{5,}'
```

结果：身份词、旧内部品牌代号、真实广告 ID/联系方式全部 **zero hit**；联系方式正则仅剩
`corpus_build.py`、`CHANGELOG.md`、`HANDOFF.md` 中对过滤规则的文档化示例（示例性占位串，非真实 ID）。
两轮原始库存 JSON（`_grouped_files.json` / `_inventory_raw.json`）中各 57 处广告联系方式已改写为中性表述。

---

## 四、局限与已知问题（如实说明）

### 4.1 本轮已修复（相对 v0.1.x）

| 旧局限 | v0.3.0 状态 |
|---|---|
| MATLAB 互操作未真机跑通 | **已修复**：R2024b 实测通过；可用工具箱 Optimization / Global Optimization / Statistics and Machine Learning / Signal Processing / Image Processing；求解器 linprog / intlinprog / fmincon / ga / ode45 全部核对预期结果 |
| xelatex 模板 loglo.png 装饰图缺陷 | **已修复**：删除装饰图块，干净目录 xelatex 双遍编译 12 页 PDF 通过，无需占位图 |
| Word 链路未端到端烟雾 | **已修复**：render_word.vbs 真机导出 8 页 PDF |
| 奖级两口径不一致（615 vs 722） | **已修复**：对账统一，729 篇自述式奖级文本扫描，索引新增 award_evidence 列 |
| 禁忌搜索泛化标签（61 篇误用） | **已修复**：61 → 4，57 篇改标"启发式优化" |
| 盲检样本不足（10 题） | **已改善**：扩到 30 题 golden set，命中率 97%（诚实标注：这是规则回归一致性，新题型泛化仍有限，见 4.2） |
| 81 条广告水印标题 / 2 张深卡 schema 违例 | v0.1.1 已修复，本轮保持零违例 |
| 环境无一键自检 | **已修复**：`scripts/doctor.py` 9 项检查，本机 8 PASS / 1 WARN |
| 无开箱样例 | **已修复**：`docs/examples/quickstart/` + `quickstart_demo.ps1` 全链路玩具样例 |
| 技术路线图无规格/渲染/校验 | **已修复**：YAML schema + 8 模板 + render/audit 脚本 + 3 demo + 12 项测试 |
| 论文成稿无系统化终检清单（v0.4.0） | **已修复**：138 条优秀论文自检表结构化固化（JSON + 人读版）+ `paper_checklist.py` 机检 + P4/P6 门禁闭环（自检表文件 / 已勾选表 / sidecar 未裁决计数） |
| 2 张深卡 schema 违例 | v0.1.1 已修复，本轮保持零违例（729 简卡 + 80 深卡全部零违例） |

### 4.2 残余局限（本轮未解决，诚实保留）

1. **Deep Learning Toolbox license 已探测但仍 unavailable**：`matlab_smoke.m` 新增 license 探测块，本机 R2024b 实测 DL license unavailable；深度学习相关求解链路未实跑（非本仓库问题，是 license 未签出）。
2. **2025 档案全 C 级证据**：2025 年赛道档案证据等级均为 C（商业/自媒体资料），权威性弱。
3. **多模态视觉复核奖级未做**：约 ≤40 篇奖级判定待人工视觉复核（716 篇仍标"待确认"）。
4. **2024 B/D 扫描版思路未全量 OCR**：仅 2024 A 做了 OCR，B/D 仍是扫描件未抽文。
5. **49 个 mp4 未转写**：视频讲解尚未转文字入库。
6. **盲检 97% 的泛化边界**：该数字是 30 题规则回归一致性，对全新题型/新表述的泛化能力未验证；1 xfailed（2017_B）即暴露此边界。
7. **reference.bib 注释语法**：模板 `reference.bib` 用 LaTeX 风格 `%` 注释，bibtex 不识别该注释符，编译时会报 5 条 warning/error（不阻塞 12 页 PDF 产出）；用户替换为真实文献时顺手把注释改成 `@comment{...}` 或删掉即可。
8. **figure-atlas PNG（19 张/13MB）已入库**：低于 50MB 阈值按计划提交；如需瘦身可后续排除。
9. **CI 未真机跑过**：`.github/workflows/ci.yml` 已写好但尚未 push 触发；doctor 步骤在无 xelatex/MATLAB 的 Ubuntu runner 上预期会 FAIL（已设 continue-on-error 不阻塞）。
10. ~~F03 二三级标题字体冲突待你裁决~~ **已按官方宋体定稿**（2026-09-21 用户裁决）：第三方自检表原写"小四黑体"不采纳，以官方格式规范为准（小四宋体），`gmcmthesis.cls`/Word 模板本就如此、未改模板（见 `modules/paper-writing.md` §6.1）。
11. ~~自检表 3 处转录存疑~~ **原图转录已确认**（2026-09-21）；随后内容审计进一步纠错：M14 改为说明验证划分、指标、超参数与输出约束，I02 改为未来改进必须对应局限并标明未验证，V05 改为“验证划分服从 IID/分组/时序/空间数据结构”。原始确认记录保留为历史，不再作为通用建模规则。

---

## 五、比赛日 1-2-3 上手步骤

**1. 安装**：junction 已建立
`C:\Users\ASUS\Doubao\skills\huawei-mcm` → 仓库根，Doubao 中输入“华为杯”即可触发本 Skill。

**2. 比赛日主流程**：
```
python scripts/contest_init.py --workdir 你的工作目录        # 建五区骨架
# 把题面放入 题目/
python scripts/playbook_match.py --txt 题目/题面.txt         # 只出题型候选
# 按 modules/kickoff-audit.md 做逐段拆解 → 产出读题审计报告
# 等你（用户）确认，把确认写进 evidence-ledger
python scripts/progress.py --root 工作目录 --gate P1        # 确认记录齐全才放行
# 进入求解（solving + playbooks）→ 验证（validation）
python scripts/progress.py --root 工作目录 --gate P2        # 模型假设≥3条
# 论文：paper-writing + abstract → 终稿
python scripts/paper_checklist.py --tex 论文/main.tex --problems 题数 --archetype 题型   # 终审机检 138 条自检表
python scripts/submission_audit.py --paper 论文.pdf         # 提交前审计
```

**3. 赛前训练**：读 `modules/training.md` 与 `modules/` 各模块；按 `tracks/` 历年档案复盘；
翻阅 `playbooks/` 方法卡建立题型直觉。

---

## 六、学术诚信与免责

- 本 Skill 只做检索、结构化与流程辅助，**不承诺获奖、不代提交、不鼓励抄袭**。
- 所有 AI 生成内容须**你理解后用自己的话表述**；模型/公式来源须可追溯。
- 按当届官方规定**披露 AI 使用**（工具名/版本/开发机构/发布日期）；无法确认来源的 AI 模型/公式有不被评阅认可的风险。

## 七、版本

**v0.4.1**（2026-09-21）：在 v0.4.0 基础上完成 F03 用户裁决定稿——
二三级标题字体经用户裁决**以官方格式规范为准（小四号宋体）**，第三方自检表原写"黑体"不采纳，`gmcmthesis.cls`/Word 模板不改；
所有"冲突待裁决"状态关闭，demo 闭环证据（`paper_checklist_decisions.json` + 已勾选表）同步重生成；版本号统一升 v0.4.1。

> 上一版：**v0.4.0**（2026-09-21）Wave6 三分片集成——138 条自检表结构化固化（`assets/checklists/`）+ `paper_checklist.py` 机检 + 6 模块逐章内嵌 + P4/P6 门禁打通，87 passed / 2 skipped / 1 xfailed；当时 F03 冲突如实标注待裁决、3 处转录存疑（M14/I02/V05）已经你确认定稿。

> 历史：v0.3.0（技术路线图体系 + 对抗挑刺 + 工程化开箱 + 写作图表细节库，74 测试全绿）；
> v0.2.0（MATLAB R2024b 真机 / loglo 修复 / 30 题盲检 97% / 品牌定稿「AI 作战中枢」，58 测试全绿）；
> v0.1.x 初始发布（729 篇蒸馏 / 8 原型 / 24 赛道档案 / 20 模块）。详见 `docs/CHANGELOG.md`。
