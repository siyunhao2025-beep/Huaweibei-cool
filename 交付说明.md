# 华为杯研赛 Skill 当前交付说明

> 状态：`main` 分支的 Unreleased 加固版（基于 v0.4.1）。本文件只写当前事实；旧轮次记录见 `docs/CHANGELOG.md` 与已明确标为历史归档的 `docs/HANDOFF.md`。

本仓库是面向中国研究生数学建模竞赛（华为杯）的辅助 Skill。它提供读题、选题、建模、验证、论文、图表和提交门禁，但不替代参赛队独立思考，不承诺奖项，也不代替用户提交。

## 1. 当前资产

| 类别 | 当前事实 |
|---|---|
| 获奖论文索引 | 729 条，2004–2024，`paper_id` 唯一；索引中的 729 条 `extract_status` 均为 `ok` |
| 全文抽文 | `corpus/text/` 是本机生成资产，因版权与体积不随仓库分发；索引只保存安全的相对路径。若本机装载该目录，测试会强制核对 729 篇一篇不少 |
| 知识卡 | brief 729 张、deep 80 张、方法统计 67 类 |
| 题型与赛题 | 8 个题型原型、51 张方法卡、2022–2025 共 24 份 A–F 赛题档案 |
| 工作流 | 21 个非空模块，全部在 `SKILL.md` 路由表中登记 |
| 工程脚本 | `scripts/` 下 22 个 Python 脚本，另含 MATLAB/Word 辅助文件 |
| 论文模板 | 2026 正式提交版 LaTeX 与 Word 模板；物理首页为实名封皮但不显示页码，第二页摘要从 1 开始 |
| 配图子 Skill | 39 个已映射生产图族、19 张集中式图册预览；图与表按真实证据规划，不按数量凑篇幅 |
| 自检表 | 138 条结构化检查；不能可靠机检的主观项必须人工裁决，不能伪装成自动通过 |

## 2. 本轮验证快照

以下是 2026-09-22 在源码工作副本上的本机实测，不把脚本存在等同于运行成功：

| 验证 | 结果 |
|---|---|
| 全量 pytest | **142 passed**；0 skipped、0 xfailed、0 xpassed |
| 环境 doctor | **10 PASS / 0 WARN / 0 FAIL / 0 SKIP**；Python、依赖、XeLaTeX、中文字体、R、MATLAB、Word、Graphviz 均检出 |
| Python 静态检查 | Ruff `E9,F,B` 通过；`compileall` 通过 |
| 依赖一致性与漏洞 | `pip check` 无破损依赖；`pip-audit` 无已知漏洞 |
| 代码安全 | Bandit 中高风险 0 命中；秘密扫描 0 命中。参考包的 SHA-256 清单按“校验值而非凭据”精确豁免 |
| 配图引用完整性 | 39/39 图族映射一致，47 个面板比例键一致，0 FAIL / 0 WARN |
| 配图脚本语法 | 39/39 图族的 Python/R 生产脚本通过解析；本机 R 4.6.1 已真机参与检查 |
| 配图 QA 回归 | 47/47 断言通过，15/15 规则覆盖，0 假阳性、0 假阴性（仅指固定回归样本） |
| 图资产实绘 | 10 个华为杯建模图资产真实运行，均生成非空 PDF、PNG 与统计输出 |
| 本地触发规则 | 固定 40 条样本内 fixture 全部匹配；这不是 Codex 平台或真实用户分布的生产准确率 |
| LaTeX 最小示例 | 干净目录 XeLaTeX 双遍成功，当前输出 **3 页**；旧文档所写 12 页是另一份参考 PDF，不是生产示例页数 |
| PDF 程序审计 | A4、3 个物理页、封皮 4 个 logo、封皮无页码、摘要页码 1、无页眉身份泄漏；西文 Times New Roman 嵌入 |
| PDF 视觉审计 | 3 页逐页渲染复核，无裁切、重叠、缺图、黑块或不可读字形 |
| 安装副本 | 由提交归档重新安装；559 个文件逐项核对为 0 缺失、0 内容差异、0 多余文件；安装态全量测试 142 passed |
| GitHub CI | Ubuntu + Python 3.12 下依赖/漏洞/秘密/Bandit/Ruff/语法/pytest 各步骤全部 success；测试策略禁止 skip/xfail/xpass |

## 3. 不夸大的质量口径

- `run_ab_tests.py` 现在只做静态能力契约，不能声称“优化后质量提升”。只有用户提供同一数据、同一指标下的完整 baseline/candidate 配对测量，`ab_test.py` 才允许比较。
- `trigger_benchmark.py` 是 40 条人工编写的样本内回归 fixture，不代表 Codex 的真实技能分派准确率，也不证明外部泛化。
- Figure 数量必须先由题面、数据和证据合同推导，再由用户锁定；证据只能支撑 M 张时，应报告上限 M，而不是换图型凑到目标数。
- 当前官方材料只明确摘要一般不超过两页，未给出正文最低页数、正文上限或全文总页数上限。任何后续官方通知优先于本仓库。

## 4. 使用入口

安装到 Codex 的 skills 目录后，输入“华为杯”或使用 `$huawei-mcm`。第一次使用时按 README 信息卡填写学校、队号、三名队员、题号、阶段、已有文件和截止时间；学校、队号和姓名只进入第 0 页封皮，不得进入第二页起的匿名内容。

比赛日主线：

```text
P0 环境初始化
→ P1 逐段读题审计与用户确认
→ P2 基线与主模型
→ P3 求解、验证、证据化图表
→ P4 论文初稿与篇幅裁决
→ P5 三轮终审
→ P6 正式提交包、AI 披露与赛后蒸馏
```

主要命令：

```powershell
python scripts/doctor.py
python scripts/contest_init.py --workdir <比赛工作目录>
python scripts/playbook_match.py --txt <题面文本>
python scripts/progress.py --root <比赛工作目录> --all
python scripts/audit_tex.py --help
python scripts/audit_paper.py --help
python scripts/paper_checklist.py --help
python scripts/submission_audit.py --help
```

语料重建不再绑定个人桌面路径，必须显式指定只读源：

```powershell
python scripts/corpus_build.py --corpus-root <获奖论文语料根>
# 或先设置 HUAWEI_CORPUS_ROOT
```

## 5. 仍需诚实保留的边界

1. Overleaf 的真实云端编译尚需用户上传最终交付包复核；本机 XeLaTeX 通过不能替代 Overleaf 真机结果。
2. MATLAB Deep Learning Toolbox license 在既有真机证据中不可用；涉及深度学习时不能宣称该链路已跑通。
3. 2025 赛题档案的部分来源证据等级为 C；当届题面和官方通知到手后必须覆盖历史资料。
4. 约 716 篇论文的奖级元数据仍为待确认；不能据此做精确奖级因果结论。
5. 语料索引记录抽文成功不等于仓库分发全文；公开 Skill 不包含 729 篇全文文件。

## 6. 完成报告纪律

每轮只用三种状态：

- **implemented**：代码或文档已经落地；
- **tested**：对应验证已实际运行并通过；
- **live-validated pending**：仍需 GitHub、Overleaf、比赛系统或特定许可证环境验证。

最终 LaTeX/PDF 只有完成三轮互补检查后才能标记 `tested`：源码/自动审计；XeLaTeX 编译并逐页看图；从实际交付包或安装副本重新编译并复核页码、匿名性、字体、缺图和溢出。
